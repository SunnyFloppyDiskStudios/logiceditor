console.log("interactableItems.js");

// handler for interactions in those kinds of items (like sliders or buttons)

function addInteraction(machine) {
    // items with class of wire-interact get listeners added
}